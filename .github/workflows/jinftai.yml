name: Build mpartel/bindfs Android arm64-v8a

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y autoconf automake libtool pkg-config gettext libtinfo6

      - name: Fix NDK clang libtinfo dependency (Ubuntu 24.04)
        run: |
          sudo ln -sf /lib/x86_64-linux-gnu/libtinfo.so.6 /lib/x86_64-linux-gnu/libtinfo.so.5

      - name: Download Android NDK r25c
        run: |
          wget -q https://dl.google.com/android/repository/android-ndk-r25c-linux.zip
          unzip -q android-ndk-r25c-linux.zip
          echo "NDK_PATH=$PWD/android-ndk-r25c" >> $GITHUB_ENV

      - name: Verify NDK compiler
        run: |
          export TOOLCHAIN=$NDK_PATH/toolchains/llvm/prebuilt/linux-x86_64
          $TOOLCHAIN/bin/aarch64-linux-android21-clang --version
          echo 'int main(){return 0;}' > /tmp/test.c
          $TOOLCHAIN/bin/aarch64-linux-android21-clang /tmp/test.c -o /tmp/test
          file /tmp/test

      - name: Clone sources
        run: |
          git clone --depth=1 https://github.com/mpartel/bindfs.git
          git clone --depth=1 https://github.com/agnostic-apollo/fuse.git

      - name: Build fuse (static library)
        env:
          NDK: ${{ env.NDK_PATH }}
        run: |
          export TOOLCHAIN=$NDK/toolchains/llvm/prebuilt/linux-x86_64
          export TARGET=aarch64-linux-android
          export API=21
          export SYSROOT=$TOOLCHAIN/sysroot
          
          export AR=$TOOLCHAIN/bin/llvm-ar
          export CC=$TOOLCHAIN/bin/$TARGET$API-clang
          export CXX=$TOOLCHAIN/bin/$TARGET$API-clang++
          export RANLIB=$TOOLCHAIN/bin/llvm-ranlib
          export STRIP=$TOOLCHAIN/bin/llvm-strip
          unset LD
          
          cd fuse
          ./makeconf.sh
          ./configure \
            --host=$TARGET \
            --with-sysroot=$SYSROOT \
            --enable-example=no \
            --disable-mtab \
            --enable-android=yes \
            --enable-static=yes \
            --disable-shared \
            2>&1 || (echo "=== FUSE CONFIG.LOG ===" && cat config.log | tail -n 100 && exit 1)
          
          make -j$(nproc)
          make DESTDIR=$PWD/install install

      - name: Patch userinfo.c for Android
        run: |
          cd bindfs/src
          # 在文件开头插入 Android bionic 缺失的函数 stub（无条件插入）
          sed -i '1i\
          // Android stub functions for missing bionic functions\n\
          static struct group* getgrent(void) { return 0; }\
          static struct passwd* getpwent(void) { return 0; }\
          static void setgrent(void) {}\
          static void endgrent(void) {}\
          static void endpwent(void) {}\
          static int getgrgid_r(gid_t gid, struct group * grp, char * buf, size_t buflen, struct group ** result) { *result = 0; return 0; }\
          static int getgrnam_r(const char * name, struct group * grp, char * buf, size_t buflen, struct group ** result) { *result = 0; return 0; }' userinfo.c
          echo "=== Patched userinfo.c (first 30 lines) ==="
          head -n 30 userinfo.c

      - name: Build bindfs
        env:
          NDK: ${{ env.NDK_PATH }}
        run: |
          export TOOLCHAIN=$NDK/toolchains/llvm/prebuilt/linux-x86_64
          export TARGET=aarch64-linux-android
          export API=21
          export SYSROOT=$TOOLCHAIN/sysroot
          
          export AR=$TOOLCHAIN/bin/llvm-ar
          export CC=$TOOLCHAIN/bin/$TARGET$API-clang
          export CXX=$TOOLCHAIN/bin/$TARGET$API-clang++
          export RANLIB=$TOOLCHAIN/bin/llvm-ranlib
          export STRIP=$TOOLCHAIN/bin/llvm-strip
          unset LD
          
          export FUSE_INSTALL=$PWD/fuse/install/usr/local
          
          # 验证 FUSE 静态库和头文件是否存在
          echo "=== Checking fuse library ==="
          ls -l $FUSE_INSTALL/lib/libfuse.a || echo "libfuse.a not found"
          ls -l $FUSE_INSTALL/include/fuse/fuse.h || echo "fuse.h not found"
          
          # 设置 FUSE_CFLAGS 和 FUSE_LIBS 直接传递给 configure，跳过 pkg-config 检测
          export FUSE_CFLAGS="-I$FUSE_INSTALL/include"
          export FUSE_LIBS="-L$FUSE_INSTALL/lib -lfuse"
          
          cd bindfs
          
          ./autogen.sh
          
          # CFLAGS 和 LDFLAGS 保留静态链接，-D__ANDROID__ 确保条件编译生效
          export CFLAGS="-static -D__ANDROID__ -I$FUSE_INSTALL/include -I$FUSE_INSTALL/include/fuse"
          export LDFLAGS="-static -static-libgcc -L$FUSE_INSTALL/lib"
          
          ./configure \
            --host=$TARGET \
            --with-sysroot=$SYSROOT \
            --enable-static=yes \
            2>&1 || (echo "=== BINDFS CONFIG.LOG ===" && cat config.log | tail -n 200 && exit 1)
          
          make -j$(nproc)
          make DESTDIR=$PWD/install install
          
          # 剥离调试符号
          $STRIP install/bin/bindfs 2>/dev/null || $STRIP install/usr/local/bin/bindfs 2>/dev/null || true

